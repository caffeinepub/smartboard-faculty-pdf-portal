{
  "frontend/src/index.css": "Global styles with OKLCH academic color palette (deep navy primary, amber/gold accent, warm off-white backgrounds), Inter/Playfair Display fonts, smart board optimized base font sizes, and utility classes for touch targets and annotation canvas.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Inter/Playfair Display fonts, academic color tokens (success, warning), board-optimized font sizes, elevated shadow variants, and animation keyframes.",
  "frontend/src/context/FacultyContext.tsx": "React context providing selected faculty ID and name state with setSelectedFaculty and clearSelectedFaculty actions, used to persist faculty identity across the portal session.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for faculty, PDFs, annotations, devices, and user profiles; now also exports `useVerifyAdminCredentials` and `useSetAdminCredentials` mutations that call the backend `verifyAdminCredentials` and `setAdminCredentials` methods respectively.",
  "frontend/src/components/Layout.tsx": "Shared layout wrapper with sticky header navigation, footer, and a \"Lock\" button shown when on the admin route; updated to import and dispatch the `LOCK_EVENT` from AdminPasscodeGate instead of using navigate, so the passcode gate reacts immediately without a page navigation.",
  "frontend/src/components/PDFUploadForm.tsx": "PDF upload form updated to accept a `disabled` prop that visually disables all controls and shows a tooltip explaining the PDF limit restriction when the plan limit is reached.",
  "frontend/src/components/PDFListTable.tsx": "Table displaying all uploaded PDFs with columns for title, assigned faculty names, upload date, and taught/not-taught status badges using shadcn Table component.",
  "frontend/src/components/FacultySelector.tsx": "Faculty selection screen with a shadcn Select dropdown populated from the faculty list; triggers onSelect callback with faculty ID and name on continue.",
  "frontend/src/components/FacultyPDFList.tsx": "Card list of PDFs assigned to a specific faculty member, showing title and taught status; clicking a card navigates to the teaching view route.",
  "frontend/src/components/AnnotationToolbar.tsx": "Extended annotation toolbar with new tool buttons for rectangle, circle, triangle, arrow, straight line, background highlight, and image insertion; includes a stroke color picker (preset + custom), a background/fill color picker (preset pastels + custom), and a hidden file input for image uploads.",
  "frontend/src/components/AnnotationCanvas.tsx": "Canvas overlay component extended to support all new annotation types (rectangle, circle, triangle, arrow, line, backgroundHighlight, image) with drag-based shape drawing, arrowhead rendering, semi-transparent fill for background highlights, image placement on click, and proper redraw of all saved annotation types including the new fields (endX, endY, imageData, fillColor).",
  "frontend/src/pages/AdminPanel.tsx": "Admin panel page wrapped in AdminPasscodeGate; passes the required `facultyList` and `allPdfs` props to `FacultyManagementTable`, includes all existing sections (subscription, devices, faculty management, PDF list), and renders the `ChangeAdminCredentialsForm` in a dedicated card at the bottom.",
  "frontend/src/pages/FacultyPortal.tsx": "Faculty portal page with faculty selector and PDF list; adds a subtle \"Developer Access\" link below the faculty selector and a \"Dev Portal\" button in the header bar, both visible only to admin users and navigating to `/dev-portal`.",
  "frontend/src/pages/TeachingView.tsx": "Full-screen teaching view updated to manage fillColor state and pendingImageData state, pass new props (fillColor, onFillColorChange, onImageSelected, onImagePlaced) to AnnotationToolbar and AnnotationCanvas, and forward all new annotation fields (endX, endY, imageData, shapeType, fillColor) to the debounced save mutation.",
  "frontend/src/pages/HomePage.tsx": "Landing page updated to show default admin login credentials (username: admin, password: admin1234) in a clearly visible two-field notice card with a security reminder to change credentials after first login.",
  "frontend/src/App.tsx": "Main app with TanStack Router; wraps the entire RouterProvider and the root route component with the new ErrorBoundary to catch any render-level crashes, and adds a hint message to the loading spinner for better UX during slow ICP network connections.",
  "frontend/src/components/SubscriptionSection.tsx": "Subscription management component showing three plan cards (Basic, Premium, Diamond) with no Free plan; each card has a billing cycle selector (Monthly/Quarterly/Half-yearly/Yearly) that dynamically updates the displayed INR price, faculty/PDF limit tiles, feature list, usage progress bars for the active plan, and a select/change button that persists the chosen tier and billing cycle to localStorage.",
  "frontend/src/components/FacultyManagementTable.tsx": "New faculty management table component with columns for name, active/inactive status badge, PDF count, and action buttons (inline edit with save/cancel, deactivate/reactivate with confirmation dialog, delete with confirmation dialog) using shadcn Table, AlertDialog, and Tooltip components.",
  "frontend/src/utils/deviceFingerprint.ts": "Utility module that generates a stable device fingerprint by combining user agent, screen resolution, canvas hash, timezone, hardware concurrency, and a persisted UUID from localStorage; exports `getDeviceFingerprint()` that caches the result in localStorage for consistency across sessions.",
  "frontend/src/utils/pwa.ts": "PWA utility module exporting `usePWAInstall()` hook that listens for the `beforeinstallprompt` event and provides `isInstallable`, `isInstalled`, and `promptInstall` state/function; also exports `registerServiceWorker()` for service worker registration.",
  "frontend/src/hooks/useDeviceRegistration.ts": "Device registration hook with a 12-second safety timeout fallback. Since the backend does not expose a `registerDevice` method in the current interface, the hook now immediately transitions to `allowed` state once the actor is ready, preventing the app from being blocked.",
  "frontend/src/components/DeviceLimitBlocker.tsx": "Full-screen blocking component rendered when the device limit is exceeded; displays a destructive-styled message explaining the 10-device limit and instructs the user to contact their administrator.",
  "frontend/src/components/DeviceManagementSection.tsx": "Admin card component showing device usage progress and a table of registered devices with remove buttons. Fixed to call `useGetDevices()` and `useGetDeviceCount()` with no arguments (matching the updated hook signatures), call `removeDevice.mutateAsync(fingerprint)` with a plain string, and use a local `DeviceRecord` interface for typed device rows.",
  "frontend/index.html": "HTML entry point updated with PWA manifest link, theme-color meta tag, Apple touch icon, and apple-mobile-web-app meta tags for installability.",
  "frontend/public/manifest.json": "Web app manifest for PWA installability with name \"EduBoard Smart Faculty Portal\", standalone display mode, amber theme color, landscape orientation, and references to the 192×192 and 512×512 PWA icons.",
  "frontend/public/service-worker.js": "Service worker implementing cache-first for static assets, network-first for navigation, and passthrough for ICP/canister API calls; handles install (precache), activate (cache cleanup), and fetch events.",
  "frontend/src/pages/DeveloperPortal.tsx": "Comprehensive admin-only Developer Portal with summary stat cards and five tabbed sections (Faculty, PDFs, Annotations, Subscription, Devices). Fixed all import names to match the actual exports from `useQueries.ts` (`useAllFacultyWithPdfCount`, `useGetDevices`, `useAllPDFs`), removed references to non-existent hooks (`useGetAllPDFsWithAnnotations`, `useGetAllAnnotations`, `useGetAllDevices`) and the non-existent `FullPDFWithAnnotations` backend type, and rewrote the PDFs/Annotations tabs to work with the available `PDF` type directly.",
  "frontend/src/components/CreateFacultyModal.tsx": "Modal dialog for creating a faculty member. The `parseBackendError` function now handles the three structured error prefixes (`LIMIT_REACHED:`, `BACKEND_ERROR:`, `CALL_ERROR:`) with specific user-friendly messages, including a distinct admin-access error with a shield icon. The modal resets name, error state, and mutation state every time it opens.",
  "frontend/src/components/ErrorBoundary.tsx": "New React class-based error boundary that catches render errors and displays a user-friendly fallback UI with the error message and a Reload Page button instead of a blank screen.",
  "frontend/src/components/AdminPasscodeGate.tsx": "Passcode gate replaced with a full username + password login form that calls the backend `verifyAdminCredentials` mutation; shows inline loading spinner during verification, displays \"Invalid username or password.\" on failure, retains sessionStorage session persistence and LOCK_EVENT listener.",
  "frontend/src/components/ChangeAdminCredentialsForm.tsx": "New form component with New Username, New Password, and Confirm Password fields; performs client-side validation (non-empty username, min 6-char password, matching passwords) and calls the backend `setAdminCredentials` mutation, showing inline success or error messages and clearing fields on success."
}