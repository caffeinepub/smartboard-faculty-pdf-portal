{
  "frontend/src/index.css": "Global styles with OKLCH academic color palette (deep navy primary, amber/gold accent, warm off-white backgrounds), Inter/Playfair Display fonts, smart board optimized base font sizes, and utility classes for touch targets and annotation canvas.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Inter/Playfair Display fonts, academic color tokens (success, warning), board-optimized font sizes, elevated shadow variants, and animation keyframes.",
  "frontend/src/context/FacultyContext.tsx": "React context providing selected faculty ID (number) and name state with setSelectedFaculty and clearSelectedFaculty actions; updated from bigint to number to match the localStorage-based Faculty type.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks and localStorage-based data persistence for faculty, PDFs, annotations, devices, and admin credentials; exports all types (Faculty, PDF, FacultyWithPdfCount, DeviceRecord, PlanTier, BillingCycle, etc.) and all CRUD mutations/queries with correct hook names and aliases.",
  "frontend/src/components/Layout.tsx": "Shared layout wrapper with sticky header navigation and footer; removed the `useIsAdmin` import entirely, using only route-path detection (`isAdminRoute`) to conditionally show the Lock button.",
  "frontend/src/components/PDFUploadForm.tsx": "PDF upload form that fetches active faculty from useAllFacultyAdmin, shows a checkbox list for faculty assignment, validates all fields, and calls useUploadPDF mutation with title/base64/selectedFacultyIds; no facultyList prop needed.",
  "frontend/src/components/PDFListTable.tsx": "Table displaying all uploaded PDFs with columns for title, assigned faculty names, upload date, and taught/not-taught status; uses Faculty and PDF types from useQueries with number IDs.",
  "frontend/src/components/FacultySelector.tsx": "Faculty selection screen using Faculty type from useQueries with number IDs; triggers onSelect callback with numeric faculty ID and name.",
  "frontend/src/components/FacultyPDFList.tsx": "Card list of PDFs assigned to a specific faculty member using number IDs; uses usePDFsByFaculty hook and navigates to the teaching view with numeric pdfId and facultyId params.",
  "frontend/src/components/AnnotationToolbar.tsx": "Extended annotation toolbar with new tool buttons for rectangle, circle, triangle, arrow, straight line, background highlight, and image insertion; includes a stroke color picker (preset + custom), a background/fill color picker (preset pastels + custom), and a hidden file input for image uploads.",
  "frontend/src/components/AnnotationCanvas.tsx": "Canvas overlay component supporting all annotation types (draw, highlight, text, eraser, rectangle, circle, triangle, arrow, line, backgroundHighlight, image); updated to use LocalAnnotation from useQueries instead of the backend Annotation type for savedAnnotations prop.",
  "frontend/src/pages/AdminPanel.tsx": "Admin panel page wrapped in AdminPasscodeGate; imports Faculty and PLAN_TIERS from useQueries, fixes FacultyCreateResult variant property access (limit/message), and passes no facultyList prop to PDFUploadForm.",
  "frontend/src/pages/FacultyPortal.tsx": "Faculty portal page with faculty selector and PDF list; updated handleSelect signature from bigint to number to match the FacultySelector and FacultyContext types.",
  "frontend/src/pages/TeachingView.tsx": "Full-screen teaching view using numeric pdfId/facultyId params; uses usePDFsByFaculty, useAnnotationsByPDF, useSaveAnnotation, and useMarkAsTaught hooks with number types throughout.",
  "frontend/src/pages/HomePage.tsx": "Landing page updated to show default admin login credentials (username: admin, password: admin1234) in a clearly visible two-field notice card with a security reminder to change credentials after first login.",
  "frontend/src/App.tsx": "Main app with TanStack Router; wraps the entire RouterProvider and the root route component with the new ErrorBoundary to catch any render-level crashes, and adds a hint message to the loading spinner for better UX during slow ICP network connections.",
  "frontend/src/components/SubscriptionSection.tsx": "Subscription plan selector showing Basic/Premium/Diamond cards with billing cycle selectors, INR pricing, usage progress bars, and localStorage persistence — no authentication logic.",
  "frontend/src/components/FacultyManagementTable.tsx": "Faculty management table with inline edit, activate/deactivate toggle, and delete actions; imports Faculty and PDF types from useQueries (not backend) and uses useDeactivateFaculty/useReactivateFaculty hooks.",
  "frontend/src/utils/deviceFingerprint.ts": "Utility module that generates a stable device fingerprint by combining user agent, screen resolution, canvas hash, timezone, hardware concurrency, and a persisted UUID from localStorage; exports `getDeviceFingerprint()` that caches the result in localStorage for consistency across sessions.",
  "frontend/src/utils/pwa.ts": "PWA utility module exporting `usePWAInstall()` hook that listens for the `beforeinstallprompt` event and provides `isInstallable`, `isInstalled`, and `promptInstall` state/function; also exports `registerServiceWorker()` for service worker registration.",
  "frontend/src/hooks/useDeviceRegistration.ts": "Device registration hook with a 12-second safety timeout fallback. Since the backend does not expose a `registerDevice` method in the current interface, the hook now immediately transitions to `allowed` state once the actor is ready, preventing the app from being blocked.",
  "frontend/src/components/DeviceLimitBlocker.tsx": "Full-screen blocking component rendered when the device limit is exceeded; displays a destructive-styled message explaining the 10-device limit and instructs the user to contact their administrator.",
  "frontend/src/components/DeviceManagementSection.tsx": "Admin card showing device usage progress and registered devices table; uses correct hook names useGetDevices, useGetDeviceCount, useRemoveDevice and DeviceRecord with fingerprint field.",
  "frontend/index.html": "HTML entry point updated with PWA manifest link, theme-color meta tag, Apple touch icon, and apple-mobile-web-app meta tags for installability.",
  "frontend/public/manifest.json": "Web app manifest for PWA installability with name \"EduBoard Smart Faculty Portal\", standalone display mode, amber theme color, landscape orientation, and references to the 192×192 and 512×512 PWA icons.",
  "frontend/public/service-worker.js": "Service worker implementing cache-first for static assets, network-first for navigation, and passthrough for ICP/canister API calls; handles install (precache), activate (cache cleanup), and fetch events.",
  "frontend/src/pages/DeveloperPortal.tsx": "Comprehensive developer portal with summary stat cards and four tabbed sections (Faculty, PDFs, Devices, Plan); all types imported from useQueries, uses correct hook names, and DeviceRecord.fingerprint field throughout.",
  "frontend/src/components/CreateFacultyModal.tsx": "Modal dialog for creating a faculty member; resets name, error, and mutation state via `useEffect` every time the modal opens (`open === true`), calls `useAddFaculty` with only the trimmed faculty name (no admin credential), closes immediately on success, and displays backend error messages without any special admin-access error variant.",
  "frontend/src/components/ErrorBoundary.tsx": "New React class-based error boundary that catches render errors and displays a user-friendly fallback UI with the error message and a Reload Page button instead of a blank screen.",
  "frontend/src/components/AdminPasscodeGate.tsx": "Full-screen admin login gate using localStorage-based credential verification (no backend call); stores session in sessionStorage and listens for LOCK_EVENT to clear it.",
  "frontend/src/components/ChangeAdminCredentialsForm.tsx": "Form for updating admin username and password using the useSetAdminCredentials hook from useQueries (localStorage-based)."
}